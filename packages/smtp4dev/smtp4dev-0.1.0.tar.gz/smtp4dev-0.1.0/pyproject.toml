[tool.poetry]
name = "smtp4dev"
version = "0.1.0"
description = "Provides access to emails caught by the SMTP4dev development server"
authors = ["Guillaume Pasquet <guillaume.pasquet@plentific.com>"]

[tool.poetry.dependencies]
python = ">=3.6"
requests = "^2.23.0"

[tool.poetry.dev-dependencies]
pytest = "^4.6"
black = "^19.10b0"
pylint = "^2.4.4"

[build-system]
requires = ["poetry>=1.0"]
build-backend = "poetry.masonry.api"
