[tool.poetry]
name = "poetry-ci-updater"
version = "0.1.0"
description = ""
authors = ["MarcoGlauser"]
license = "MIT"
readme = "README.md"
homepage = "https://github.com/MarcoGlauser/poetry-ci-updater"
repository = "https://github.com/MarcoGlauser/poetry-ci-updater"
keywords = ["poetry", "ci", "auto updater"]

[tool.poetry.dependencies]
python = "^3.6"
gitpython = "^3.0.0"
requests = "^2.22.0"
click = "^7.0"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"

[tool.poetry.scripts]
poetry-ci-updater = 'poetry_ci_updater.main:main'