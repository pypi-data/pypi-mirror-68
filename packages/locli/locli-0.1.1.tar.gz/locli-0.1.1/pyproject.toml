[tool.poetry]
name = "locli"
version = "0.1.1"
description = ""
authors = ["wonjae.h <warren@telelian.com>"]

[tool.poetry.dependencies]
python = "^3.7"
redis = "^3.5.0"
loguru = "^0.4.1"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[tool.poetry.scripts]
exam1 = "examples.exam1:main"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
