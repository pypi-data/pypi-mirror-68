[tool.poetry]
name = "zealous"
version = "0.1.0"
description = "Zealous Framework"
authors = ["Jonathan Gravel <un@stashed.dev>"]

[tool.poetry.dependencies]
python = "^3.7"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
