[tool.poetry]
name = "az-rip"
version = "0.0.1"
description = "az-rip"
authors = ["Stefano Borini"]
packages = [
    { include = "rip", from = "src" },
]

[tool.poetry.dependencies]
python = "^3.7"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
