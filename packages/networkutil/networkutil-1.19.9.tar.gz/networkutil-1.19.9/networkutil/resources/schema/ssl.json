{
  "$schema": "http://json-schema.org/schema#",
  "description": "Schema for PyHTTPIntercept SSL Certificate config",

  "definitions": {
    "certificate": {
      "type": "object",
      "properties": {
        "key": {"type": "string"},
        "pub": {"type": "string"},
        "csr": {"type": "string"},
        "crt": {"type": "string"},
        "cer": {"type": "string"}
      },
      "additionalProperties": false
    }
  },

  "type": "object",
  "additionalProperties": { "$ref": "#/definitions/certificate" }
}