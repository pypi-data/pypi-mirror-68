{
  "$schema": "http://json-schema.org/schema#",
  "description": "Schema for networkutil jumpbox host config",

  "definitions": {

    "jump_host": {
      "type": "object",
      "properties": {
        "host": {"type": "string"},
        "port": {"type": "number"},
        "elevated_user": {"type": "string"}
      },
      "required": ["host", "port"],
      "additionalProperties": false
    }

  },

  "type": "object",
  "additionalProperties": { "$ref": "#/definitions/jump_host" }
}