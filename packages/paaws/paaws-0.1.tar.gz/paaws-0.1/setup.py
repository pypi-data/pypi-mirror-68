#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['paaws', 'paaws.cli']

package_data = \
{'': ['*']}

install_requires = \
['awscli', 'awslogs', 'blessed', 'boto3', 'click', 'halo', 'timeago']

extras_require = \
{'dev': ['shiv']}

entry_points = \
{'console_scripts': ['paaws = paaws.__main__:main']}

setup(name='paaws',
      version='0.1',
      description='Deploy control for ECS',
      author='Peter Baumgartner',
      author_email='pete@lincolnloop.com',
      url='https://github.com/lincolnloop/paaws',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      extras_require=extras_require,
      entry_points=entry_points,
      python_requires='>=3.6',
     )
